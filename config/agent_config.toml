# model = "phi3"
model = "codellama:34b"
max_attempts = 20
log_file = "monty_ollama_agent.log"
log_level = "DEBUG"

[prompts]
universal_rules = """
IMPORTANT — OUTPUT FORMAT RULES (must be followed exactly):
- Output ONLY raw Python code. NOTHING ELSE.
- NO markdown fences (no ```python, no ```, no backticks)
- NO explanations, NO comments, NO # lines, NO docstrings
- NO extra text before, inside or after the code
- The first character of your response must be valid Python
- End your code by assigning the final answer to a variable called 'result'
- You may use print(result) or print(...) for debugging, but 'result' is required
"""

monty_constraints = """
Monty supports ONLY:
- variables, assignment, math ops (+ - * / % **)
- if/elif/else
- for/while loops (simple)
- def functions (sync or async)
- async/await (limited asyncio)
- type hints (typing)
- print()
- list/dict/tuple/set literals & operations
- return

Forbidden:
- input(), open(), sys, any I/O except print()
- classes, decorators, with, match/case
- imports except typing/asyncio (limited)
- __name__ == "__main__", asyncio.run()
- chained assignment (x = y = 5)
"""